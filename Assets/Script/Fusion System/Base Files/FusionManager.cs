using System.Collections.Generic;
using UnityEngine;

//need this if not gg code 
//atp idk if this code is even efficient anymore bruh 
public class FusionDataWrapper : ScriptableObject {}

public class FusionManager : MonoBehaviour
{

    [SerializeField] private List<FusionDataWrapper> list; 
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
  
    }

    // Update is called once per frame
    void Update()
    {
        
    }



}
